export enum HttpStatusCode {
    权限错误 = 210,
    token过期 = 220,
    用户不存在 = 230,
    要注册的用户已经存在 = 240,
    参数不足 = 400,
}

export enum LocalPath {
    novel = "/novel",
    novelSrc = "/novel/src",
    novelHtml = "/novel/html",
    novelPreview = "/novel/preview",
}
